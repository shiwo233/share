# 仓库自动化运营规范（AGENTS.md）

> 本文件是此仓库的"操作手册"，供 Kimi Claw / Kimi 或任何自动化 Agent 阅读执行。
> 仓库主人：shiwo233 ｜ 仓库用途：研究报告永久存档 + 知识图谱 Wiki + 议题更新追踪

## 一、仓库结构约定

```
/                       ← 根目录（保留原有 index.html，勿动）
/six-tigers-report/     ← 议题一：《中国大模型六小虎深度研究》交互网页
/wiki/index.html        ← 知识图谱 Wiki 主页（议题索引 + 更新动态栏）
/wiki/updates.json      ← 更新动态数据源（唯一事实源，最新条目在最前）
/AGENTS.md              ← 本文件
/<新议题目录>/index.html ← 每个新议题一个目录，自包含单文件交互网页
```

## 二、定期巡检任务（建议周期：每天一次）

对每个标记"追踪中"的议题执行：

1. **检索**：围绕该议题的监测项搜索最新资讯（如六小虎议题：模型新版本发布、榜单名次变化、融资/上市/股价异动、核心人物动态）。
2. **判断**：与议题页面和 `updates.json` 已有内容比对，只记录**实质性新增信息**，无更新则跳过，不要注水。
3. **写入**：若有更新——
   - 在 `wiki/updates.json` 的 `updates` 数组**最前面**追加条目，格式：
     ```json
     {"date": "YYYY-MM-DD", "title": "一句话标题", "topic": "议题名", "body": "2–3 句摘要，含关键数据"}
     ```
   - 若更新影响议题正文结论，同步修订该议题页面的对应段落。
4. **提交**：commit message 格式 `更新：<议题名> - <一句话摘要>`，推送到 `main` 分支。

## 三、新议题入库流程

当主人完成一份新研究报告时：

1. 在仓库根目录新建 `<议题英文名>/index.html`（移动端优先的深色交互网页，风格参照 `/six-tigers-report/index.html`）。每篇报告**必须包含**：① 与 Wiki 主页同风格的顶部导航栏（布局：**本报告章节锚点居左，「主页」链接 + W logo 居右**，均链接 `../wiki/`）；② 「下一篇推荐」模块（**整体置于页面底部、footer 之前**，通栏样式参照 `.nextup`；2026-08-12 起主人确认改为页底布局，不再使用桌面端右上角悬浮 / 移动端顶部横幅）。
2. 在 `wiki/index.html` 的 `TOPICS` 数组中追加该议题（cat/url/icon/date/hot + 中/英双语的 title/desc/watch/tags/**toc 目录数组**——悬停预览会显示目录）。
3. 在 `wiki/updates.json` 最前追加入库动态（含 title_en/body_en 英文字段）。
4. 提交并推送，确认 GitHub Pages 可访问。

## 四、风格与质量要求

- 页面风格：深色主题（#0a0e15 底，蓝紫金渐变点缀），移动端优先，含交互模块。
- 内容要求：所有数据须来自可核实的公开来源；未上市公司数据标注"报道/传闻"口径；不做投资建议。
- 语言：中文为主，术语保留英文原文。

## 五、凭据

- GitHub 凭据通过环境变量 `GITHUB_TOKEN` 注入（细粒度 PAT，仅限本仓库 Contents 读写），**绝不要将 token 写入任何仓库文件**。
